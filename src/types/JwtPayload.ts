export type JwtPayload = {
  id: string;
};
export type FixtureDto = {
  home: string;
  away: string;
  venue: string;
  status: string;
}

export type TeamDto = {
  name: string;
  code: string;
  venue: string;
  status: string;
}
